* move both docker compose files to this repo and possibly into ansible dir
* change activity log to log email (primary key)
  * create a small script and attempt to migrate the old data to have primary key
* change main user database to have email function as primary key and also require unique card uuids and student ids